# Database (M4 — PostgreSQL)

Not started yet. Schema will be designed from the Python entity classes in
`simulation/src/entities/` once there are enough of them (citizen, family,
tradition, ...) to model properly — so the schema matches the simulation
instead of the other way around.
