# No third-party packages needed yet.
# The simulation engine uses only the Python standard library so far
# (including for tests, via `unittest`). Add packages here only when a
# later step genuinely needs one, and note why in a commit message.
